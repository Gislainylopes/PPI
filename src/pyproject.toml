[project]
name = "music_app"
version = "1.0.0"
dependencies = [
    "flask",
    "werkzeug",
    "sqlite3",
    "click"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"